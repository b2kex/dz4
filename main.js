function cont(count) {
let min = document.querySelector('.minus');
let pl = document.querySelector('.plus');
let number = document.querySelector('.number');
let numberValue = parseFloat(number.Value, 10);

min.addEventListener('click', () => {console.log(input.value - 1)} );
pl.addEventListener('click', () => {console.log(number + 1)});
}
